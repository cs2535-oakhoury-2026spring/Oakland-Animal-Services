# Instructions

## Tasks:

1. Write the documentations of the methods in `StaffObservationService.ts` according the functionality of the methods:
    - `createBehavioralNote()` - takes in parsed inputs from the staff and creates a behavioral note and passes it to the repository for storage
    - `createMedicalNote()` - takes in parsed inputs from the staff and creates a medical note and passes it to the repository for storage
    - `getNotesByDate()` - takes in a date and returns all notes for that date
    - `getNotesByAnimalId()` - takes in an animal ID and returns all notes for that animal

2. Write the documentations of the methods in `VetObservationService.ts` according the functionality of the methods:
    - `resolveMedicalNote()` - takes a medical note and resolves it