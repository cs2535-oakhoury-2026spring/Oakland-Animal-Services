Store pending ADRs here until its ready to be merged and assigned a number. This is for ADRs that are still being drafted or reviewed and not yet finalized. Once an ADR is ready, move it to the main `docs/adr` directory and assign it a sequential number (e.g., ADR-08.md). This also helps avoid merge conflicts from different branches working on different ADRs that might got assigned the similar numbers.